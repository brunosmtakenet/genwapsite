<header>
	<!-- <h2>This is the header.</h2> -->
	<div>
		<ul id="pagelist">
			<li><%= link_to 'New Page', new_page_path %></li>
			<% @pages.each do |page| %>
				<li><%= link_to page.title, edit_page_path(page) %></li>
			<% end %>
		</ul>
	</div>
	
</header>